# 分布式调度总览：

┌─────────────────────────────────────────────────────────────────────┐
│                          主服务器 (Master)                           │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │
│  │  FastAPI    │  │  任务队列   │  │  文件监控   │  │  PostgreSQL │ │
│  │  (调度器)   │──│   (Redis)   │──│  (Watchdog) │──│   数据库    │ │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘ │
│         │                │                │                         │
│         ▼                ▼                ▼                         │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                     NAS 共享文件夹                            │   │
│  │  ├── tasks/          (任务分发)                              │   │
│  │  ├── results/        (爬取结果)                              │   │
│  │  └── processed/      (已处理)                                │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┼───────────────┐
                    ▼               ▼               ▼
            ┌───────────┐   ┌───────────┐   ┌───────────┐
            │  Worker 1 │   │  Worker 2 │   │  Worker 3 │
            │ (虚拟机)  │   │ (虚拟机)  │   │ (虚拟机)  │
            │ 代理池 A  │   │ 代理池 B  │   │ 代理池 C  │
            └───────────┘   └───────────┘   └───────────┘



# 文件状态：

我已经仔细检查了你的所有项目文件。整体来说项目结构清晰、功能完整，但有一些问题需要修复和优化。

## 📋 文件检查结果汇总

### ✅ 正常/良好的文件

| 文件 | 状态 | 说明 |
|------|------|------|
| `docker-compose.yml` | ✅ 良好 | 配置正确，NAS 挂载、健康检查都 OK |
| `init.sql` | ✅ 良好 | 表结构完整，索引、触发器齐全 |
| `nginx.conf` | ✅ 良好 | 代理配置正确 |
| `style.css` | ✅ 良好 | 样式完整，包含动画 |
| `common.js` | ✅ 良好 | API 基础配置正确 |
| `dashboard.js` | ✅ 良好 | 仪表盘逻辑正确 |
| `data.js` | ✅ 良好 | 数据查询完整，ASIN筛选、位置计算都有 |
| `keywords.js` | ✅ 良好 | 关键词管理完整 |
| `tasks.js` | ✅ 良好 | 任务监控完整，运行中任务、详情都有 |
| `README.md` | ✅ 良好 | 文档完整详细 |
| `项目总览.md` | ✅ 良好 | 结构清晰 |
| `调度总览.md` | ✅ 良好 | 分布式架构图清晰 |

### ⚠️ 需要修复的文件

| 文件 | 问题 | 严重程度 |
|------|------|---------|
| `scrape.js` | `lastRunningTasks` 变量未定义 | 🔴 严重 |
| `scraping.py` | WebSocket 代码未删除（有 403 错误）| 🟡 中等 |
| `app.js` | 存在但未被使用（与模块化 JS 重复）| 🟢 轻微 |
| `data copy.js` | 备份文件不应存在 | 🟢 轻微 |
| `start.sh` | Windows 下无法直接运行 | 🟢 轻微 |

---

## 🔧 需要修复的问题

### 问题1：`scrape.js` 中 `lastRunningTasks` 未定义

**位置**：`frontend/js/scrape.js` 第 2 行后

**当前代码**：
```javascript
let logPollingInterval = null;
// 缺少 lastRunningTasks 定义
```

**修复方案**：
```javascript
let logPollingInterval = null;
let lastRunningTasks = [];  // 添加这一行
```

### 问题2：`scraping.py` 中 WebSocket 代码未删除

**位置**：`backend/app/api/scraping.py`

**问题**：WebSocket 代码会导致 403 错误

**修复方案**：替换为无 WebSocket 版本

```python
# backend/app/api/scraping.py
from fastapi import APIRouter, BackgroundTasks, Query
from typing import Optional

router = APIRouter()


@router.post("/scrape")
def start_scraping(
    background_tasks: BackgroundTasks,
    keyword: Optional[str] = Query(None),
    pages: Optional[int] = Query(None)
):
    """启动爬取任务"""
    from app.scraper.pipeline import run_now
    
    background_tasks.add_task(run_now, keyword, pages)
    
    return {
        "message": "任务已启动",
        "keyword": keyword or "所有关键词",
        "pages": pages or "自动"
    }


@router.post("/scrape/daily")
def trigger_daily(background_tasks: BackgroundTasks):
    """触发每日任务"""
    from app.scraper.pipeline import run_daily
    
    background_tasks.add_task(run_daily)
    return {"message": "每日任务已启动"}


@router.post("/scrape/weekly")
def trigger_weekly(background_tasks: BackgroundTasks):
    """触发每周任务"""
    from app.scraper.pipeline import run_weekly
    
    background_tasks.add_task(run_weekly)
    return {"message": "每周任务已启动"}
```

### 问题3：删除不需要的文件

```bash
# 删除备份文件
rm frontend/js/data\ copy.js

# 删除未使用的 app.js（如果确认不使用）
# 注意：检查 index.html 等是否引用了 app.js
# 如果没引用，可以删除
```

### 问题4：`start.sh` 在 Windows 下无法运行

**解决方案**：创建 `start.bat` 用于 Windows

```batch
@echo off
REM start.bat - Windows 启动脚本

echo 启动 Docker 服务...
docker-compose up -d

echo 等待后端启动...
timeout /t 10 /nobreak

echo 重启前端以确保连接...
docker restart amazon_frontend

echo 服务已启动！
echo 前端: http://localhost:8880
echo 后端: http://localhost:8888
pause
```

---

## 📁 建议的文件清理

### 可选删除（不影响功能）

```
frontend/js/
├── app.js           # 未被使用（如果确认没被引用）
├── data copy.js     # 备份文件，应删除
```

### 建议保留（被使用）

```
frontend/js/
├── common.js        ✅ 被所有页面使用
├── dashboard.js     ✅ index.html 使用
├── keywords.js      ✅ keywords.html 使用
├── scrape.js        ✅ scrape.html 使用
├── data.js          ✅ data.html 使用
├── tasks.js         ✅ tasks.html 使用
```

---

## 🚀 启动流程

### 1. 先修复 `scrape.js`

在 `frontend/js/scrape.js` 第 2 行后添加：
```javascript
let lastRunningTasks = [];
```

### 2. 修复 `scraping.py`

替换为无 WebSocket 版本

### 3. 重启服务

```bash
# 重启后端
docker restart amazon_backend

# 重启前端
docker restart amazon_frontend

# 或者完全重建
docker-compose down
docker-compose up -d --build
```

### 4. 验证

```bash
# 测试 API
curl http://localhost:8888/health
curl http://localhost:8888/api/keywords

# 访问前端
http://localhost:8880
```

---

## 📊 整体评价

| 维度 | 评分 | 说明 |
|------|------|------|
| 项目结构 | ⭐⭐⭐⭐⭐ | 清晰完整 |
| 代码质量 | ⭐⭐⭐⭐ | 有少量问题需修复 |
| 功能完整性 | ⭐⭐⭐⭐⭐ | 爬取、存储、查询、监控齐全 |
| 文档完整性 | ⭐⭐⭐⭐⭐ | README 非常详细 |
| Docker 配置 | ⭐⭐⭐⭐⭐ | 配置正确，支持 NAS 挂载 |

**修复上述 2 个问题后，项目即可正常运行！**